import React from 'react';
import SetTitle from '../../Utilities/SetTitle/SetTitle';

const Home = () => {
    return (
        <div>
            {/* title */}
            <SetTitle>Home</SetTitle>
        </div>
    );
};

export default Home;